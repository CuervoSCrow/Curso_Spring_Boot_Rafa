package org.mock.MockTestingProject01;

import org.mock.MockTestingProject01.persistence.entity.Player;
import org.mock.MockTestingProject01.persistence.repository.PlayerRepositoryImpl;
import org.mock.MockTestingProject01.persistence.service.PlayerServiceImpl;
import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class MockTestingProject01Application {

	public static void main(String[] args) {
//		SpringApplication.run(MockTestingProject01Application.class, args);
//        PlayerRepositoryImpl playerRepository = new PlayerRepositoryImpl();
//        PlayerServiceImpl playerService = new PlayerServiceImpl(playerRepository);
//
////        System.out.println(playerService.findAll());
////        System.out.println(playerService.findById(1L));
////        playerService.deleteById(1L);
////        System.out.println(playerService.findAll());
//        Player player=new Player(7L,"Luis Diaz", "Liverpool", "Delantero");
//        playerService.save(player);
//        System.out.println(playerService.findAll());
	}

}
